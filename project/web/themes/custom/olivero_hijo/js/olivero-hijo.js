/**
 * @file
 * olivero_hijo behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.oliveroHijo = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
